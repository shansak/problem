#include<stdio.h>
#include<conio.h>
int max(int a[],int b[],int c[], int a1,int a2,int a3)
{
  int i, sum1=0,sum2=0,sum3=0;
  for(i=0;i<=a1;i++)
   sum1=sum1+a[i];
  for(i=0;i<=a2;i++)
    sum2=sum2+b[i];
  for(i=0;i<=a3;i++)
    sum3=sum3+c[i];
    
 
 if(sum1==sum2&&sum2==sum3)
  return 0;
 else if(sum1>sum2&&sumsum1>sum3)
   sum1=sum1-a[top];
 else if(sum2>sum1&&sum2>sum3)
    sum2=sum2-b[top];
  else(sum3>sum1&&sum3>sum2)
     sum3=sum3-c[top];
  
  
  
  
}
int main()
{
  
